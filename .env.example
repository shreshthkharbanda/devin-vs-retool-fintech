# Supabase project this API reads/writes on behalf of Retool.
SUPABASE_URL=
SUPABASE_SECRET_KEY=

# Shared-secret header Retool's REST resource must send (X-Demo-Key).
# Not real security — just keeps the tunneled demo URL from being wide open.
DEMO_API_KEY=

PORT=3000
