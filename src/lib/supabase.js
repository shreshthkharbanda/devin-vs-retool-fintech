// Responsibility: construct and export a single Supabase client using
// SUPABASE_URL + SUPABASE_SECRET_KEY (service-role — bypasses RLS).
// This is the only file in the project allowed to reference the secret key.
// Nothing outside this module should import @supabase/supabase-js directly.
