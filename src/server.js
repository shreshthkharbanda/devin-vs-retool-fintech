// Entry point.
// Responsibility: load env, create the app, apply auth + actor middleware
// globally, mount /api/kyc-cases and /api/audit-log routers, start listening
// on PORT. No business logic lives here.
